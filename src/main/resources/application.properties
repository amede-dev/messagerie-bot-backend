spring.application.name=messagerie-bot

# --- Connexion Supabase (Session Pooler, IPv4-compatible) ---
spring.datasource.url=jdbc:postgresql://aws-0-ap-south-1.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.rmctacrltqieporqrait
spring.datasource.password=JNOnyDg195gMI6nj
spring.datasource.driver-class-name=org.postgresql.Driver

# 'update' en developpement uniquement. Une fois le projet stable,
# passer a une vraie gestion de migrations (Flyway/Liquibase) plutot
# que de laisser Hibernate modifier le schema automatiquement.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=8080

# Cle utilisee pour signer les tokens JWT.
# (utiliser une variable d'environnement JWT_SECRET a la place).
app.jwt.secret=${JWT_SECRET:changez-cette-cle-secrete-avant-la-production-0123456789}
app.jwt.expiration-ms=86400000

logging.level.mg.eni.reseauuniversitaire.messageriebot=DEBUG